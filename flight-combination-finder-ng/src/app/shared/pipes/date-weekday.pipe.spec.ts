import { DateWeekdayPipe } from './date-weekday.pipe';

describe('DateWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
