import { AirlineColorPipe } from './airline-color.pipe';

describe('AirlineColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AirlineColorPipe();
    expect(pipe).toBeTruthy();
  });
});
